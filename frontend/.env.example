# Copy this file to .env and fill in real values. .env is gitignored — never
# commit real values.

# Base URL of the backend API (see backend/.env.example). For local dev this
# is the backend's own address + /api — no change needed unless you moved
# the backend to a different host/port.
VITE_API_BASE_URL=http://localhost:5000/api
